---
const { page = 'home' } = Astro.props;
const isHome = page === 'home';
const appStoreUrl = 'https://apps.apple.com/us/app/around-the-house-binder/id6789646793';
const prefix = isHome ? '' : '/';

const links = [
  ['Features', `${prefix}#features`],
  ['Screenshots', '/screenshots/'],
  ['Pricing', '/pricing/'],
  ['Support', '/support/']
];
---
<header class="site-header">
  <nav class="container nav" aria-label="Main navigation">
    <a class="brand" href="/" aria-label="Around The House home">
      <img class="brand-mark" src="/assets/icons/around-the-house-icon.png" width="512" height="512" alt="" />
      <span>Around The House</span>
    </a>
    <div class="nav-links desktop-links">
      {links.map(([label, href]) => <a href={href}>{label}</a>)}
      <a class="nav-download" href={appStoreUrl} target="_blank" rel="noopener">Download on the App Store</a>
    </div>
    <details class="nav-disclosure">
      <summary aria-label="Open menu">Menu</summary>
      <div class="nav-disclosure-panel">
        {links.map(([label, href]) => <a href={href}>{label}</a>)}
        <a class="nav-download" href={appStoreUrl} target="_blank" rel="noopener">Download on the App Store</a>
      </div>
    </details>
  </nav>
</header>
